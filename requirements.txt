# Exact versions, matching the Mac this was built and tested on.
# Unpinned ('>=') let GitHub Actions install anthropic 1.7.0, which removed the
# 'temperature' argument and made every extraction fail. Pinning keeps the
# Action on exactly the code the tests and the 20-article review ran against.
# Upgrade deliberately, one package at a time, and re-run the tests.
requests==2.32.5
feedparser==6.0.12
beautifulsoup4==4.15.0
lxml==6.1.3
PyYAML==6.0.3
python-dateutil==2.9.0.post0
anthropic==0.125.0
gspread==6.2.1
google-auth==2.50.0
numpy==2.0.2
